#posts.transitions-enabled
	- @posts.each do |post|
		.box.panel.panel-default
			= link_to (image_tag post.image.url), post, class:"fancybox"
			.panel-body
				%h2= link_to post.title, post
				%p.user
				= post.user.name
				%p.round-image-50= image_tag(post.user.avatar.url(:thumb))